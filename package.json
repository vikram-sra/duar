{
  "name": "duar.one",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && rm -rf docs && cp -r dist docs && touch docs/.nojekyll",
    "preview": "vite preview",
    "portfolio": "python3 scripts/build-portfolio.py"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  },
  "dependencies": {
    "gsap": "^3.14.2",
    "three": "^0.182.0"
  }
}
